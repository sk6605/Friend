export function useConfetti() {
    const fire = () => {
        // Placeholder for actual confetti logic
        // We could import 'canvas-confetti' later
        console.log('Confetti fired! 🎉');
    };
    return { fire };
}
